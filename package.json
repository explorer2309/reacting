{
  "name": "transporter",
  "version": "1.0.0",
  "description": "transporter using node and react",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pb",
  "license": "ISC",
  "dependencies": {
    "react": "^0.14.2"
  },
  "devDependencies": {
    "browserify": "^12.0.1"
  }
}
