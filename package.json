{
  "name": "transporter",
  "version": "1.0.0",
  "description": "transporter using node and react",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/client/app.js -t babelify -o src/client/bundle.js"
  },
  "author": "pb",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.14.1",
    "compression": "^1.6.0",
    "express": "^4.13.3",
    "react": "^0.14.2"
  },
  "devDependencies": {
    "babelify": "^7.2.0",
    "browserify": "^12.0.1"
  }
}
